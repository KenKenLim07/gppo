# Firebase Admin SDK Cleanup Script Environment Variables
# Replace the values below with your actual Firebase configuration

# Your Firebase Realtime Database URL (already known)
FIREBASE_DATABASE_URL=https://gppo-tracker-default-rtdb.asia-southeast1.firebasedatabase.app/

# Choose ONE of these two options for service account authentication:

# Option 1: Path to your service account JSON file (recommended)
# Download from Firebase Console → Project Settings → Service Accounts → Generate New Private Key
GSERVICE_ACCOUNT_PATH=/Users/mac/Downloads/gppo-service-account.json

# Option 2: Inline service account JSON (alternative)
# Uncomment and paste your service account JSON here if you prefer inline
# GSERVICE_ACCOUNT_JSON={"type":"service_account","project_id":"gppo-tracker",...}

# Optional: Set to 'true' if you also want to clean emergency notifications
CLEAN_EMERGENCY_NOTIFICATIONS=false